server:
  port: 8086
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/SafariComExamDB
    username: postgres
    password: forbiden
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
